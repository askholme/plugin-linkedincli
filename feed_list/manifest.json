{
  "name": "feed_list",
  "description": "List posts from the LinkedIn feed.",
  "entrypoint": "run.py",
  "parameters": {
    "count": {
      "type": "string",
      "description": "Number of posts to return (default: 10)"
    },
    "start": {
      "type": "string",
      "description": "Offset to start from (default: 0)"
    },
    "sort": {
      "type": "string",
      "description": "Feed sort order: `relevance` (default) or `recent`."
    },
    "cursor": {
      "type": "string",
      "description": "Pagination cursor for `sort=recent`. Omit to start from the newest page."
    },
    "pages": {
      "type": "string",
      "description": "Number of recent-feed pages to fetch when `sort=recent` (default: 1)."
    }
  }
}
