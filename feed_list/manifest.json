{
  "name": "feed_list",
  "description": "List posts from the LinkedIn feed.",
  "entrypoint": "run.py",
  "parameters": {
    "count": {
      "type": "string",
      "description": "Number of posts to return (default: 10)"
    },
    "start": {
      "type": "string",
      "description": "Offset to start from. When paginating with `cursor`, set to the previous response's `paging.start + paging.count`."
    },
    "sort": {
      "type": "string",
      "description": "Feed sort order: `relevance` (default) or `recent`."
    },
    "cursor": {
      "type": "string",
      "description": "Pagination cursor token from a prior call's `paging.next_cursor`. Omit to start from the first page."
    }
  }
}
