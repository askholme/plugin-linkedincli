{
  "name": "notifications_list",
  "description": "List LinkedIn notifications.",
  "entrypoint": "run.py",
  "parameters": {
    "count": {
      "type": "string",
      "description": "Number of notifications to return (default: 10)"
    },
    "start": {
      "type": "string",
      "description": "Offset to start from (default: 0)"
    }
  }
}
